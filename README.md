**Задача на многопоточку с одного из собеседований. Уровень сложности средний. (но пытливые начинающие разработчики тоже могут попробовать).**

**Условие: нужно реализовать сервис, который будет отправлять средства с одного счета на другой. Двумя способами: через пессемистичную стратегию блокировок (через блокировки) и оптимистичиный подход (lock-free) — без блокировок:**

```java
    /**
     *  сигнатуру менять нельзя. Вместо знака вопроса нужно подставить тип, который посчитаете нужным.
     */
public static boolean transferMoney(Account from, Account to, ? amount) 
```

**Также бонусный вопрос такой**: подумай в каком случае нужно применить пессемистичный подход ? А Оптимистичный?  Какие подходы и паттерны можно использовать для организации отказоустойчивости?
